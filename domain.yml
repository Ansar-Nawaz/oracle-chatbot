version: "3.1"

intents:
  - ask_error
  - greet

entities:
  - error_code  # Required for ORA-XXXXX extraction

responses:
  utter_greet:
    - text: "Hello! Ask me about Oracle errors (e.g., ORA-00942)."

actions:
  - action_fetch_solution
