version: "3.1"  # Update from 3.0 to 3.1

intents:
  - ask_error   # Ensure intents are explicitly listed
  - greet

responses:
  utter_greet:
    - text: "Hello! How can I help with Oracle errors today?"

actions:
  - action_fetch_solutionk
