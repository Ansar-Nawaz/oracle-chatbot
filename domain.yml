version: "3.1"

intents:
  - ask_error
  - greet

responses:
  utter_greet:
    - text: "👋 Hi! I'm your Oracle error assistant. What's broken?"

actions:
  - action_fetch_solution

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
